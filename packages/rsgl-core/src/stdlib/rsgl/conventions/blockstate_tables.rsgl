template stateSequence(key: String, values: List<Json> | Range, model: (Json) -> ModelId) -> variants {
    for value in values {
      case { [key]: value } => model(value)
    }
}

export { stateSequence }
