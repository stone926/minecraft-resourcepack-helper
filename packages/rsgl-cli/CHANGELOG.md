# Changelog

## [Unreleased]

## [1.0.0]

- Publish the RSGL compiler as the independently versioned `@minecraft-resourcepack-helper/rsgl-cli` npm package.
- Support `init`, `build`, `check`, and `watch` project workflows.
- Bundle the compiler, project configuration support, and RSGL standard library into the installable command-line artifact.
