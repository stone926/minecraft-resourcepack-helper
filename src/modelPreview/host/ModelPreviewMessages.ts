/** Compatibility facade for host callers; the protocol itself is host/webview-neutral. */
export type {
  HostToWebview,
  ModelPreviewError,
  ScreenshotOptions,
  WebviewLocalizedMessage,
  WebviewToHost
} from "../protocol/ModelPreviewProtocol";
