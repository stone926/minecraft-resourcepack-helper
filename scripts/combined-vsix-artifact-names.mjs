/** Stable filenames shared by VSIX assembly and measurement pipelines. */
export const combinedVsixArtifactNames = Object.freeze({
  development: "combined-unminified.vsix",
  production: "combined-production.vsix"
});
