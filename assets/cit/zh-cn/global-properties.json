{
  "version": 1,
  "id": "global-properties",
  "scope": "global",
  "globalPriority": [
    "citresewn/cit.properties",
    "optifine/cit.properties",
    "mcpatcher/cit.properties"
  ],
  "keys": {
    "useGlint": {
      "valueType": "boolean",
      "scope": ["global"],
      "default": "true",
      "singleton": true,
      "title": "使用默认光效",
      "description": "若为 false，则从所有物品上移除默认附魔光效纹理。"
    },
    "cap": {
      "valueType": "positiveInteger",
      "scope": ["global"],
      "singleton": true,
      "title": "层数上限",
      "description": "控制每个物品允许的最大 CIT 附魔光效层数。"
    },
    "method": {
      "valueType": "enum",
      "enum": ["average", "layered", "cycle"],
      "scope": ["global"],
      "default": "average",
      "singleton": true,
      "title": "方法",
      "description": "决定 CIT 附魔光效的可见性控制方式。"
    },
    "fade": {
      "valueType": "nonNegativeNumber",
      "scope": ["global"],
      "default": "0.5",
      "singleton": true,
      "title": "淡入淡出",
      "description": "当 method 为 cycle 时，控制 CIT 附魔之间的过渡时长。"
    },
    "root_fallback": {
      "valueType": "boolean",
      "scope": ["global"],
      "default": "false",
      "singleton": true,
      "title": "根目录后备",
      "description": "为 true 时，citresewn 根目录中的 CIT 会自动后备到匹配的 optifine 或 mcpatcher 根目录 CIT，除非已显式设置 fallback。"
    }
  },
  "patterns": {},
  "rules": []
}
